import {StyleSheet} from 'react-native';
import {hp} from '../../helper/utils';

export const style = StyleSheet.create({
  containerStyle: {},
  iconStyle: {
    width: hp(2),
    height: hp(2),
  },
});
